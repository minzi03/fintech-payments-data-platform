extends: default

ignore: |
  .git/
  .venv/
  .pytest_cache/
  data/
  tmp/

rules:
  document-start: disable
  line-length:
    max: 120
    level: warning
  truthy:
    allowed-values:
      - "true"
      - "false"
    check-keys: false
